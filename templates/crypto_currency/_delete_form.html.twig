<form method="post" action="{{ path('app_crypto_currency_delete', {'id': crypto_currency.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ crypto_currency.id) }}">
    <button class="btn">Delete</button>
</form>
